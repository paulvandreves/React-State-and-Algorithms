import { useEffect, useRef } from 'react'

/**
 * usePrevious stores the previous value across renders.
 * Common use case: compare current vs. previous values to detect changes.
 *
 * @template T
 * @param {T} value - Current value to track
 * @returns {T | undefined} - The value from the previous render
 */
export function usePrevious(value) {
  throw new Error('Not implemented')
}

